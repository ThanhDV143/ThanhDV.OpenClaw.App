# Scripts

Shared helper scripts can live here later.

Examples:

- Local plugin build helpers.
- Deployment checks.
- Data validation utilities.

Keep feature-specific scripts under the relevant `capabilities/<name>` folder unless they are reused by multiple capabilities.

